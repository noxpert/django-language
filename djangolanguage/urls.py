"""
Django URL configuration for djangolanguage application.

This module defines URL patterns for the djangolanguage app.
"""

from django.urls import path
from . import views

app_name = 'djangolanguage'

urlpatterns = [
    # Add your URL patterns here
    # Example:
    # path('', views.index, name='index'),
    # path('<int:id>/', views.detail, name='detail'),
]
